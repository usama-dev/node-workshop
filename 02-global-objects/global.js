// The global object provides variables and functions that are available anywhere.
// Node has a global object called 'global' whereas in Browsers it's called 'window'

// You can check the properties attached to the global object
console.log(global);